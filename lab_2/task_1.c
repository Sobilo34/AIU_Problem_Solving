#include <stdio.h>

/**
 * Student name: Bilal Oyeleke Soliu
 * Student ID: AIU25102458
 * main: The Entry point function
 * Purpose: store 20 in the A variable
 * stores 35 in the B variable
 * add the A and B variables
 * and store the result in the C variable.
 * Subtract the A and B variables and store the result in the D variable.
 */

int main() {
	int A, B, C, D;

	A = 20;
	B = 35;
	C = A+B;
	D = B-A;
	printf("C is %d\n", C);
	printf("D is %d\n", D);

	return 0;
}

