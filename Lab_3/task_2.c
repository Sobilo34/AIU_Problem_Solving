#include <stdio.h>

#define LENGTH  8
#define WIDTH  3

/**
 * Student Name: Bilal Oyeleke Soliu
 * Student ID: AIU25102458
 * main : Entry point function
 * Return: Success
 */

int main() {
	int area = LENGTH * WIDTH;

	printf("For a rectangle, it legnth is %dcm and it width is %dcm\n",LENGTH, WIDTH);
	printf("The area of rectangle is %dcm\n", area);

	return 0;
}
